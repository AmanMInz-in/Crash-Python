first_name=("aman")
last_name=("minz")
full_name=f"{first_name} {last_name}"
print(full_name)

# it seem complicated at first but here "f" is termed as format in this code 
# which defines 
# the format of that vatiable int the code which can be print later

full_name=f"hello, {first_name} {last_name}"
print(full_name)

# here we have given the format of hello with the name 

print("Student information portal by format featuer ")

name=("aman minz")
std=("Btech 1st sem")
rollno=("12621301")

student_info=f"{name.title()} class {std} roll_no {rollno}"
print(student_info) 
