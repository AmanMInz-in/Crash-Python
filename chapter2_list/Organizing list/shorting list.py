car=["bmw","audi","toyota","subaru"]
car.sort()
print(car)

# here the short() is used to sort the list in alphabatical order but it does not change the original list

# now using the shorted() function to sort the list but it does not change the original list
print("using the sorted() function to sort the list")
car=["bmw","audi","toyota","subaru"]
print(sorted(car))
# we can use also reverse the list

print("now we will reverse the list")
car.reverse()
print(car)